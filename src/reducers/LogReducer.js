// eslint-disable-next-line import/no-anonymous-default-export
export default (state, acition ) => {
    switch (action.type) {
        case value:
            return 
        default:
            return state
    }
}